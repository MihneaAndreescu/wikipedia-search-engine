# Agent Rules
- One empty line is allowed as a separator.
- Never use two consecutive empty lines.
- Around assignment `=`, always use one space on both sides: `a = b`.
- Never write assignment spacing as `a=b`, `a =b`, or `a= b`.
- Apply these rules consistently in all edited files.
